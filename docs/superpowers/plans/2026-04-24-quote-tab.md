# Quote 탭 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** bandsustain.com `/quote` 탭을 DB-backed editorial zigzag 레이아웃으로 구현한다. 각 quote 는 4:5 portrait(또는 placeholder) + blockquote + 선택적 번역 + attribution + permalink 복사 버튼. 최신순 전체 렌더.

**Architecture:** Next.js 16 App Router 서버 컴포넌트 + mysql2 **lazy** pool + MariaDB. `/quote` 페이지는 `force-dynamic` 으로 per-request DB 쿼리. 클라이언트 JS 는 permalink 복사 버튼 (`CopyPermalink.tsx`) 하나만. zigzag 는 flex-direction 으로 홀/짝 index 스위치.

**Tech Stack:** Next 16 + React 19 + TypeScript + Tailwind v4 + mysql2 (신규) + MariaDB.

**Source spec:** `docs/superpowers/specs/2026-04-24-quote-tab-design.md`

**Testing approach:** 이 프로젝트는 test runner (jest/vitest) 가 없고 `*.test.*` 파일도 없다 — members-page plan 과 동일 패턴 적용. 검증은 `pnpm lint` + `pnpm build` (타입·ESLint) + §12 수동 브라우저 체크리스트. TDD 대신 implement → `pnpm build` → commit 사이클.

**선행 조건 (spec §11):** 아래 3개가 반드시 갖춰져 있어야 구현 착수. 하나라도 빠지면 Task 1 에서 중단하고 운영자에게 핸드오프.
- MariaDB `BANDSUSTAIN` 데이터베이스 존재
- DB user 가 `BANDSUSTAIN.*` SELECT/INSERT/UPDATE/DELETE 권한 보유
- `/var/www/html/_______site_BANDSUSTAIN/.db_credentials` 존재 (chmod 600, `DB_HOST`/`DB_USER`/`DB_PASS`/`DB_NAME`)

---

## File Structure

| File | Responsibility | Type |
|---|---|---|
| `db/schema/001_quotes.sql` | `quotes` 테이블 DDL | 마이그레이션 파일 (수동 실행) |
| `db/seed/quotes_seed.sql` | 초기 3~5 건 샘플 INSERT | 시드 파일 (수동 실행, 선택적) |
| `src/lib/db.ts` | mysql2 lazy pool + `.db_credentials` 로더 | 서버 전용 유틸 |
| `src/lib/quotes.ts` | `Quote` 타입 + `getPublishedQuotes()` | 서버 전용 데이터 접근 |
| `src/components/QuotePortrait.tsx` | portrait_url 분기 (Image vs initial placeholder) | 서버 컴포넌트 |
| `src/components/CopyPermalink.tsx` | `#` 버튼 + clipboard 복사 + 가시성 규칙 | **Client** 컴포넌트 (유일) |
| `src/components/QuoteRow.tsx` | 한 quote 렌더 (zigzag, meta row 포함) | 서버 컴포넌트 |
| `src/app/quote/page.tsx` | `/quote` 페이지 — header + quote 리스트 | 서버 컴포넌트 (`force-dynamic`) (기존 "Coming soon" 교체) |
| `package.json` | `mysql2` 의존성 추가 | 기존 파일 |

---

## Task 1: 운영 선행 조건 점검 (코드 작성 없음)

**Files:** 없음 (환경 점검만)

- [ ] **Step 1: `.db_credentials` 파일 존재 확인**

Run:
```bash
ls -la /var/www/html/_______site_BANDSUSTAIN/.db_credentials
```

Expected: 파일 존재, permission `-rw-------` (chmod 600).

- [ ] **Step 2: 파일 내 키 확인 (값 출력 금지, 키 이름만)**

Run:
```bash
awk -F= '/^[A-Z_]+=/ {print $1}' /var/www/html/_______site_BANDSUSTAIN/.db_credentials | sort
```

Expected (순서 무관):
```
DB_HOST
DB_NAME
DB_PASS
DB_USER
```

- [ ] **Step 3: DB 접속 및 권한 확인**

Run:
```bash
sudo -n true 2>/dev/null && SUDO=sudo || SUDO=
eval "$($SUDO cat /var/www/html/_______site_BANDSUSTAIN/.db_credentials)"
mysql -h "$DB_HOST" -u "$DB_USER" -p"$DB_PASS" -e "USE $DB_NAME; SHOW TABLES;"
```

Expected: 접속 성공, 테이블 목록 (아직 `quotes` 없어도 정상).

- [ ] **Step 4: 위 3개 중 하나라도 실패하면 여기서 멈추고 운영자에게 보고**

구현 코드가 DB/user/credentials 를 생성하지 않는다 (spec §11). 사용자에게 다음과 같이 보고:

> "§11 선행 작업 중 `<실패한 항목>` 미충족. 운영자가 해당 항목 준비 후 다시 시작해 주세요. 구현 착수하지 않음."

- [ ] **Step 5: 선행 조건 전부 OK 일 때만 다음 Task 진행**

이 Task 는 커밋 없음 (환경 점검).

---

## Task 2: mysql2 의존성 추가

**Files:**
- Modify: `package.json`, `pnpm-lock.yaml`

- [ ] **Step 1: 패키지 설치**

Run (프로젝트 루트에서):
```bash
cd /root/bandsustain/public_html/bandsustain
pnpm add mysql2
```

Expected: `package.json` `dependencies` 에 `"mysql2": "^3.x.x"` 추가, `pnpm-lock.yaml` 갱신.

- [ ] **Step 2: 설치 확인**

Run:
```bash
node -e "console.log(require('mysql2/package.json').version)"
```

Expected: 3.x.x 버전 출력.

- [ ] **Step 3: 커밋**

```bash
git add package.json pnpm-lock.yaml
git commit -m "Add mysql2 dependency for quote tab"
```

---

## Task 3: 스키마 + 시드 SQL 파일 작성

**Files:**
- Create: `db/schema/001_quotes.sql`
- Create: `db/seed/quotes_seed.sql`

- [ ] **Step 1: 스키마 파일 작성**

Create `db/schema/001_quotes.sql`:

```sql
-- 001_quotes.sql
-- bandsustain.com /quote 탭 — 가짜 명언 저장 테이블
-- 수동 실행: mysql -h $DB_HOST -u $DB_USER -p $DB_NAME < db/schema/001_quotes.sql

CREATE TABLE IF NOT EXISTS quotes (
  id              INT AUTO_INCREMENT PRIMARY KEY,
  text            TEXT           NOT NULL,
  lang            ENUM('ko','en') NOT NULL,
  text_translated TEXT           NULL,
  attribution     VARCHAR(120)   NULL,
  portrait_url    VARCHAR(255)   NULL,
  published       TINYINT(1)     NOT NULL DEFAULT 1,
  created_at      TIMESTAMP      NOT NULL DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_published_created (published, created_at DESC)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

- [ ] **Step 2: 시드 파일 작성 (3~5 건 초기 샘플)**

Create `db/seed/quotes_seed.sql`:

```sql
-- quotes_seed.sql
-- bandsustain.com /quote 탭 초기 샘플 데이터
-- 수동 실행: mysql -h $DB_HOST -u $DB_USER -p $DB_NAME < db/seed/quotes_seed.sql
-- ※ 실행 전 사용자에게 내용 확인받고, 원하는 경우 문구를 수정한다.

INSERT INTO quotes (text, lang, text_translated, attribution, portrait_url, published) VALUES
  ('좋은 곡을 듣는다는 것은, 좋은 삶을 살고 있다는 뜻입니다.',
   'ko', NULL, '서스테인', NULL, 1),
  ('아무것도 하지 않을 때, 우리는 가장 많은 것을 하고 있다.',
   'ko', NULL, '서스테인, 밴드 리더', '/members/member01.jpg', 1),
  ('Don''t do today what you can put off until tomorrow.',
   'en', '내일로 미룰 수 있는 일은 오늘 하지 마라.', 'John, age unknown', NULL, 1),
  ('배고프면 먹어라. 졸리면 자라. 그게 전부다.',
   'ko', NULL, '이름 모를 할아버지', NULL, 1),
  ('Every great song is secretly a shopping list.',
   'en', NULL, 'anonymous', NULL, 1);
```

- [ ] **Step 3: 사용자에게 시드 문구 확인**

사용자에게 다음과 같이 물어본다:

> "`db/seed/quotes_seed.sql` 에 초기 5건 샘플 넣어뒀습니다. 내용 보시고 바꾸고 싶으시면 말씀해주세요. 그대로 가도 되면 진행하겠습니다."

사용자가 수정 요청 시 반영 후 다음 단계로.

- [ ] **Step 4: 파일만 커밋 (DB 실행은 Task 4)**

```bash
git add db/schema/001_quotes.sql db/seed/quotes_seed.sql
git commit -m "Add quote tab schema and seed SQL files"
```

---

## Task 4: 스키마 + 시드를 DB 에 적용

**Files:** 없음 (DB 상태 변경)

- [ ] **Step 1: 스키마 실행**

Run:
```bash
cd /root/bandsustain/public_html/bandsustain
eval "$(cat /var/www/html/_______site_BANDSUSTAIN/.db_credentials)"
mysql -h "$DB_HOST" -u "$DB_USER" -p"$DB_PASS" "$DB_NAME" < db/schema/001_quotes.sql
```

Expected: 출력 없음 (정상), exit 0.

- [ ] **Step 2: 테이블 생성 확인**

Run:
```bash
mysql -h "$DB_HOST" -u "$DB_USER" -p"$DB_PASS" "$DB_NAME" -e "DESCRIBE quotes;"
```

Expected: 8 columns (`id`, `text`, `lang`, `text_translated`, `attribution`, `portrait_url`, `published`, `created_at`) + index 확인.

- [ ] **Step 3: 시드 실행**

Run:
```bash
mysql -h "$DB_HOST" -u "$DB_USER" -p"$DB_PASS" "$DB_NAME" < db/seed/quotes_seed.sql
```

Expected: 출력 없음, exit 0.

- [ ] **Step 4: 시드 로우 확인**

Run:
```bash
mysql -h "$DB_HOST" -u "$DB_USER" -p"$DB_PASS" "$DB_NAME" -e "SELECT COUNT(*) FROM quotes;"
```

Expected: 5 (또는 사용자가 수정했다면 해당 건수).

- [ ] **Step 5: 커밋 없음**

이 단계는 DB 상태 변경이지 코드 변경이 아님.

---

## Task 5: `src/lib/db.ts` — lazy mysql2 pool

**Files:**
- Create: `src/lib/db.ts`

- [ ] **Step 1: 파일 작성**

Create `src/lib/db.ts`:

```ts
import mysql from "mysql2/promise";
import { readFileSync } from "node:fs";

function loadCreds(): Record<string, string> {
  const path = process.env.DB_CREDENTIALS_PATH
    ?? "/var/www/html/_______site_BANDSUSTAIN/.db_credentials";
  const raw = readFileSync(path, "utf8");
  const out: Record<string, string> = {};
  for (const line of raw.split("\n")) {
    const m = line.match(/^([A-Z_]+)=(.*)$/);
    if (m) out[m[1]] = m[2].trim().replace(/^["']|["']$/g, "");
  }
  return out;
}

function createPool(): mysql.Pool {
  const c = loadCreds();
  return mysql.createPool({
    host: c.DB_HOST,
    user: c.DB_USER,
    password: c.DB_PASS,
    database: c.DB_NAME,
    connectionLimit: 5,
    waitForConnections: true,
    charset: "utf8mb4",
  });
}

const g = globalThis as unknown as { __bs_pool?: mysql.Pool };

export function getPool(): mysql.Pool {
  return g.__bs_pool ?? (g.__bs_pool = createPool());
}
```

**중요:** 모듈 import 시점에 `readFileSync` 호출이나 풀 생성이 없어야 한다 (빌드 안전성). `getPool()` 첫 호출 시에만 `createPool()` 실행.

- [ ] **Step 2: 타입 체크**

Run:
```bash
pnpm tsc --noEmit
```

Expected: 에러 0. `src/lib/db.ts` 관련 오류 없음.

- [ ] **Step 3: import-without-connect 검증**

Run (DB 연결 없이 모듈만 로드되는지 확인):
```bash
node -e "
  require('ts-node/register');
  const m = require('./src/lib/db.ts');
  console.log('Module loaded, keys:', Object.keys(m));
"
```

만약 `ts-node` 없으면 생략 가능 — Task 11 (build safety) 에서 동등하게 검증됨.

Expected (실행했을 경우): `Module loaded, keys: [ 'getPool' ]`. DB 접속 에러 없음.

- [ ] **Step 4: 커밋**

```bash
git add src/lib/db.ts
git commit -m "Add lazy mysql2 pool at src/lib/db.ts"
```

---

## Task 6: `src/lib/quotes.ts` — 데이터 접근

**Files:**
- Create: `src/lib/quotes.ts`

- [ ] **Step 1: 파일 작성**

Create `src/lib/quotes.ts`:

```ts
import "server-only";
import type { RowDataPacket } from "mysql2";
import { getPool } from "./db";

export type Quote = {
  id: number;
  text: string;
  lang: "ko" | "en";
  text_translated: string | null;
  attribution: string | null;
  portrait_url: string | null;
  created_at: Date;
};

export async function getPublishedQuotes(): Promise<Quote[]> {
  const [rows] = await getPool().query<(Quote & RowDataPacket)[]>(
    `SELECT id, text, lang, text_translated, attribution, portrait_url, created_at
     FROM quotes
     WHERE published = 1
     ORDER BY created_at DESC, id DESC`
  );
  return rows;
}
```

- [ ] **Step 2: 타입 체크**

Run:
```bash
pnpm tsc --noEmit
```

Expected: 에러 0.

- [ ] **Step 3: 커밋**

```bash
git add src/lib/quotes.ts
git commit -m "Add getPublishedQuotes query function"
```

---

## Task 7: `QuotePortrait` 컴포넌트

**Files:**
- Create: `src/components/QuotePortrait.tsx`

- [ ] **Step 1: 파일 작성**

Create `src/components/QuotePortrait.tsx`:

```tsx
import Image from "next/image";

type Props = {
  portraitUrl: string | null;
  attribution: string | null;
};

function resolvePlaceholder(attribution: string | null): string {
  if (attribution == null) return "★";
  const trimmed = attribution.trim();
  const lower = trimmed.toLowerCase();
  if (lower === "anonymous" || trimmed === "익명") return "★";
  const first = trimmed.charAt(0);
  return /[a-zA-Z]/.test(first) ? first.toUpperCase() : first;
}

export default function QuotePortrait({ portraitUrl, attribution }: Props) {
  if (portraitUrl) {
    return (
      <Image
        src={portraitUrl}
        alt={attribution ?? "portrait"}
        width={160}
        height={200}
        className="w-full aspect-[4/5] object-cover grayscale contrast-[1.05]"
      />
    );
  }

  const glyph = resolvePlaceholder(attribution);
  return (
    <div className="w-full aspect-[4/5] bg-[var(--color-bg-muted)] flex items-center justify-center">
      <span className="font-display font-black text-4xl md:text-5xl text-[var(--color-border-strong)] opacity-30">
        {glyph}
      </span>
    </div>
  );
}
```

**Note:** `portraitUrl` 은 spec §3 규칙상 `/` 로 시작하는 public-root 경로여야 한다. 원격 URL 이 들어와도 이 컴포넌트는 막지 않는다 — 유효성은 INSERT 단계 운영자 책임 (spec §8).

- [ ] **Step 2: 타입 체크**

Run:
```bash
pnpm tsc --noEmit
```

Expected: 에러 0.

- [ ] **Step 3: 커밋**

```bash
git add src/components/QuotePortrait.tsx
git commit -m "Add QuotePortrait with image/placeholder variants"
```

---

## Task 8: `CopyPermalink` 클라이언트 컴포넌트

**Files:**
- Create: `src/components/CopyPermalink.tsx`

- [ ] **Step 1: 파일 작성**

Create `src/components/CopyPermalink.tsx`:

```tsx
"use client";

import { useState } from "react";

type Props = {
  quoteId: number;
};

export default function CopyPermalink({ quoteId }: Props) {
  const [copied, setCopied] = useState(false);

  const onClick = async () => {
    const url = `${window.location.origin}/quote#q${quoteId}`;
    try {
      await navigator.clipboard.writeText(url);
      setCopied(true);
      setTimeout(() => setCopied(false), 1500);
    } catch {
      // clipboard API 거부됨 — 조용히 실패
    }
  };

  return (
    <button
      type="button"
      onClick={onClick}
      aria-label="Copy permalink to this quote"
      className="text-xs uppercase tracking-[0.08em] text-[var(--color-text-muted)] hover:text-[var(--color-text)] opacity-60 active:opacity-100 md:opacity-0 md:group-hover:opacity-100 md:focus-visible:opacity-100 md:transition-opacity"
    >
      {copied ? "copied" : "#"}
    </button>
  );
}
```

**가시성 규칙 (spec §6 QuoteRow):**
- `< md` (모바일/터치): 기본 `opacity-60`, 눌렀을 때 `opacity-100`
- `≥ md` (데스크탑): 기본 숨김, row hover 시 또는 버튼 focus 시에만 노출

- [ ] **Step 2: 타입 체크**

Run:
```bash
pnpm tsc --noEmit
```

Expected: 에러 0.

- [ ] **Step 3: 커밋**

```bash
git add src/components/CopyPermalink.tsx
git commit -m "Add CopyPermalink client component with mobile/desktop visibility rules"
```

---

## Task 9: `QuoteRow` 컴포넌트

**Files:**
- Create: `src/components/QuoteRow.tsx`

- [ ] **Step 1: 파일 작성**

Create `src/components/QuoteRow.tsx`:

```tsx
import type { Quote } from "@/lib/quotes";
import QuotePortrait from "./QuotePortrait";
import CopyPermalink from "./CopyPermalink";

type Props = {
  quote: Quote;
  index: number;
};

export default function QuoteRow({ quote, index }: Props) {
  const reverse = index % 2 === 1;
  const topBorder = index === 0
    ? "border-t border-[var(--color-text)]"
    : "border-t border-[var(--color-border)]";
  const translationLang = quote.lang === "ko" ? "en" : "ko";

  return (
    <article
      id={`q${quote.id}`}
      lang={quote.lang}
      className={`group scroll-mt-24 ${topBorder} py-8 md:py-10 md:flex md:gap-8 md:items-center ${reverse ? "md:flex-row-reverse" : ""}`}
    >
      <div className="mb-4 md:mb-0 md:w-40 md:flex-shrink-0">
        <QuotePortrait
          portraitUrl={quote.portrait_url}
          attribution={quote.attribution}
        />
      </div>

      <div className={`md:flex-1 ${reverse ? "md:text-right" : ""}`}>
        <blockquote className="font-display font-extrabold text-xl md:text-2xl leading-[1.18] tracking-[-0.01em]">
          &ldquo;{quote.text}&rdquo;
        </blockquote>

        {quote.text_translated && (
          <p
            lang={translationLang}
            className="mt-3 text-sm md:text-base text-[var(--color-text-muted)]"
          >
            &ldquo;{quote.text_translated}&rdquo;
          </p>
        )}

        <div
          className={`mt-5 flex items-center gap-3 ${reverse ? "md:justify-end" : ""}`}
        >
          {quote.attribution && (
            <span className="text-xs uppercase tracking-[0.08em] text-[var(--color-text-muted)]">
              — {quote.attribution} —
            </span>
          )}
          <CopyPermalink quoteId={quote.id} />
        </div>
      </div>
    </article>
  );
}
```

**레이아웃 메모:**
- Mobile (`< md`): flex 비활성 → 블록으로 쌓임. portrait DOM 에서 먼저라 위에 옴. text 가 아래. `reverse` 무시됨.
- Desktop (`≥ md`): flex. `reverse=false` → portrait 왼쪽 / text 오른쪽. `reverse=true` → `flex-row-reverse` 로 portrait 오른쪽 / text 왼쪽. text 는 `md:text-right` 로 우측 정렬.
- Meta row 는 attribution 유무와 무관하게 항상 렌더 (CopyPermalink 자리 확보).

- [ ] **Step 2: 타입 체크**

Run:
```bash
pnpm tsc --noEmit
```

Expected: 에러 0.

- [ ] **Step 3: 커밋**

```bash
git add src/components/QuoteRow.tsx
git commit -m "Add QuoteRow with zigzag layout and meta row"
```

---

## Task 10: `/quote` 페이지 재작성

**Files:**
- Modify: `src/app/quote/page.tsx`

- [ ] **Step 1: 기존 "Coming soon" 페이지 전면 교체**

Overwrite `src/app/quote/page.tsx`:

```tsx
import type { Metadata } from "next";
import { getPublishedQuotes } from "@/lib/quotes";
import QuoteRow from "@/components/QuoteRow";

// 빌드 시점 pre-render 차단 — DB 미접속 환경에서도 pnpm build 가 성공해야 함.
// 저트래픽 탭이라 per-request DB 쿼리는 비용 문제 없음.
export const dynamic = "force-dynamic";

const description =
  "These are words that don't really help in life — 삶에 별로 도움은 되지 않을 명언들";
const ogImage = "/slides/hero-c28a7f43.jpg";

export const metadata: Metadata = {
  title: "Quote",
  description,
  openGraph: {
    type: "website",
    siteName: "Band Sustain",
    url: "https://bandsustain.com/quote",
    title: "Quote — Band Sustain",
    description,
    images: [{ url: ogImage, alt: "Quote" }],
    locale: "ko_KR",
  },
  twitter: {
    card: "summary_large_image",
    title: "Quote — Band Sustain",
    description,
    images: [ogImage],
  },
};

export default async function QuotePage() {
  const quotes = await getPublishedQuotes();

  return (
    <section className="max-w-7xl mx-auto px-6 md:px-12 py-16 md:py-24">
      <header className="mb-10 md:mb-12">
        <h1 className="font-display font-black uppercase tracking-tight text-4xl md:text-6xl mb-6">
          Quote
        </h1>
        <p className="text-lg md:text-xl text-[var(--color-text-muted)] leading-[1.5]">
          These are words that don&apos;t really help in life
          <br />
          삶에 별로 도움은 되지 않을 명언들
        </p>
      </header>

      {quotes.length === 0 ? (
        <p className="text-[var(--color-text-muted)]">
          Nothing quotable yet. / 아직 인용할 만한 말이 없습니다.
        </p>
      ) : (
        <div>
          {quotes.map((q, i) => (
            <QuoteRow key={q.id} quote={q} index={i} />
          ))}
        </div>
      )}
    </section>
  );
}
```

- [ ] **Step 2: 타입 체크**

Run:
```bash
pnpm tsc --noEmit
```

Expected: 에러 0.

- [ ] **Step 3: lint 체크**

Run:
```bash
pnpm lint
```

Expected: 에러 0 (경고 있어도 기존 코드가 깨끗하면 통과).

- [ ] **Step 4: 커밋**

```bash
git add src/app/quote/page.tsx
git commit -m "Rewrite /quote page with DB-backed rendering and force-dynamic"
```

---

## Task 11: 빌드 안전성 검증 (DB 미접속 시 빌드 성공 확인)

**Files:** 없음 (환경 테스트)

이 Task 는 spec 에서 핵심적으로 요구하는 "DB 없어도 `pnpm build` 성공" 을 실제로 검증한다.

- [ ] **Step 1: 임시로 credentials 경로를 바꿔서 "DB 없음" 시뮬레이션**

Run:
```bash
cd /root/bandsustain/public_html/bandsustain
DB_CREDENTIALS_PATH=/tmp/nonexistent-creds pnpm build
```

Expected: 빌드 **성공** (exit 0). `/quote` 라우트가 `force-dynamic` 이고 `db.ts` 가 lazy 이므로 빌드 시점에 파일 읽기 시도 없음.

만약 실패:
- 에러 메시지에 `ENOENT` 나 "credentials" 이 보이면 `db.ts` 가 top-level 에서 파일을 읽고 있는 것 → Task 5 의 lazy 구조를 다시 확인.
- 에러가 `force-dynamic` 관련이면 Next.js 버전 확인 + page.tsx 의 `export const dynamic` 문장 위치 확인.

- [ ] **Step 2: 정상 경로로 빌드 재실행**

Run:
```bash
pnpm build
```

Expected: 빌드 성공. `.next` 디렉토리 생성.

- [ ] **Step 3: 커밋 없음**

검증 단계. 필요시 메모만 남김.

---

## Task 12: Dev-server 수동 검증

**Files:** 없음 (브라우저 체크)

spec §10 수동 검증 체크리스트 실행.

- [ ] **Step 1: Dev server 기동**

Run:
```bash
cd /root/bandsustain/public_html/bandsustain
pnpm dev
```

Expected: `▲ Next.js` 배너 + `Local: http://localhost:3000` 출력. 에러 없음.

- [ ] **Step 2: 브라우저에서 `/quote` 접근**

URL: `http://localhost:3000/quote`

체크:
- [ ] HTTP 200 응답
- [ ] 헤더 `Quote` + 부제 2줄 렌더
- [ ] 5 건 (또는 시드 개수) 의 quote 렌더
- [ ] 구분선: 첫 row 는 진한 검정, 이하는 연한 회색

- [ ] **Step 3: Zigzag 확인**

- [ ] index 0 (최신): portrait **왼쪽**, text 오른쪽
- [ ] index 1: portrait **오른쪽**, text 왼쪽 (우측정렬)
- [ ] index 2: 다시 왼쪽
- [ ] index 3: 다시 오른쪽

- [ ] **Step 4: Portrait variant 확인**

- [ ] `portrait_url` 가 `/members/member01.jpg` 인 row → 실제 사진 + grayscale 필터
- [ ] `portrait_url` 가 NULL 이고 attribution 이 `"anonymous"` → `★`
- [ ] `portrait_url` 가 NULL 이고 attribution 이 `"John, age unknown"` → `J`
- [ ] `portrait_url` 가 NULL 이고 attribution 이 `"이름 모를 할아버지"` → `이`

- [ ] **Step 5: 번역·attribution 유무 확인**

- [ ] `text_translated` 있는 row → 본문 아래 작은 회색 텍스트로 번역 노출
- [ ] `text_translated` NULL 인 row → 번역 줄 없음
- [ ] `attribution` NULL 인 row (있다면) → `— —` 같은 빈 대시 나오지 않음 (attribution span 자체 생략)

- [ ] **Step 6: Permalink 동작**

- [ ] 데스크탑: row 에 hover 안 하면 `#` 버튼 안 보임. hover 하면 나타남
- [ ] 데스크탑: `#` 버튼 클릭 → `copied` 로 잠깐 바뀜 → 1.5초 뒤 `#` 로 복귀
- [ ] 브라우저 주소창에 `http://localhost:3000/quote#q3` 직접 입력 → id=3 인 row 로 스크롤
- [ ] 복사된 URL 을 주소창에 붙여넣어 테스트 — 같은 동작

- [ ] **Step 7: 모바일 폭 검증 (DevTools 반응형 모드 ~375px)**

- [ ] 모든 row 가 세로 단일 컬럼 (portrait 위 / text 아래)
- [ ] `#` 버튼이 **항상 보임** (opacity-60 상태)
- [ ] Zigzag 가 데스크탑에서만 적용되고 모바일에서는 무시됨 확인

- [ ] **Step 8: 콘솔 에러/워닝 확인**

- [ ] DevTools Console 에 빨간 에러 0
- [ ] React hydration 워닝 0
- [ ] `next/image` 도메인 관련 워닝 0

- [ ] **Step 9: 빈 테이블 empty state 확인 (선택)**

임시로:
```sql
UPDATE quotes SET published = 0;
```
→ 페이지 새로고침 → `Nothing quotable yet. / 아직 인용할 만한 말이 없습니다.` 렌더 확인 → 원복:
```sql
UPDATE quotes SET published = 1;
```

- [ ] **Step 10: Dev server 종료**

`Ctrl+C` 로 종료.

---

## Task 13: 프로덕션 배포

**Files:** 없음 (배포만)

- [ ] **Step 1: 최종 빌드**

Run:
```bash
cd /root/bandsustain/public_html/bandsustain
pnpm build
```

Expected: 성공.

- [ ] **Step 2: 원격 push (main)**

```bash
git status                              # 미커밋 변경 없는지 확인
git log origin/main..HEAD --oneline     # 푸시할 커밋 확인
git push origin main
```

Expected: 전부 push 됨. `bandsustain.com` 은 `dev/prod` 분리 없음 (routines 방식).

- [ ] **Step 3: PM2 재시작**

```bash
pm2 restart bandsustain
pm2 describe bandsustain | head -30
```

Expected: status `online`, uptime 방금 재시작.

- [ ] **Step 4: 프로덕션 스모크**

```bash
curl -I https://bandsustain.com/quote
```

Expected: `HTTP/2 200`.

- [ ] **Step 5: 브라우저 확인**

URL: https://bandsustain.com/quote

- [ ] Task 12 Step 2~8 의 모든 체크가 프로덕션에서도 재현됨
- [ ] 특히: 실제 모바일 폰에서 permalink 버튼 보이는지, 터치시 copied 동작 정상

- [ ] **Step 6: 이슈 발견 시**

`pm2 logs bandsustain --lines 50` 으로 런타임 에러 확인. DB 커넥션 에러는 선행 조건 (§11) 재확인.

---

## Self-review 결과

**Spec 커버리지:**
- §1 컨텍스트 / §2 결정: 플랜 전체 구조에 반영
- §3 데이터 모델: Task 3 (schema 파일) + Task 4 (DB 실행)
- §4 파일 구조: Task 5~10 + 파일 구조 표
- §5 DB 레이어: Task 5, 6
- §6 페이지·컴포넌트 렌더: Task 7, 8, 9, 10
- §7 인터랙션 (permalink): Task 8 (CopyPermalink)
- §8 에러/빈 상태: Task 12 Step 9 (empty state), Task 10 의 page.tsx (DB throw → 500)
- §9 SEO/OG: Task 10 metadata
- §10 배포/검증: Task 11, 12, 13
- §11 운영 선행 작업: Task 1
- §12 범위 밖: 각 Task 에서 암묵적 준수 (어드민·API·페이지네이션 등 구현 안 함)

**모든 §1~§13 섹션이 적어도 하나의 Task 에 대응됨.**

**자잘한 주의:**
- Task 5 Step 3 (ts-node 로 모듈 import 검증) 은 환경에 따라 생략 가능 — Task 11 의 `DB_CREDENTIALS_PATH=/tmp/nonexistent-creds pnpm build` 가 등가 검증이므로 빠져도 치명적이지 않음.
- Task 3 Step 3 에서 사용자에게 시드 문구 확인받는 단계는 사용자 부재 시 기본 문구로 진행해도 되지만, 가능하면 확인받는다.
